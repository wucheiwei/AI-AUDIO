# 前端呼叫的後端 API 網址（不要加結尾斜線）
# 這是本機開發/demo 用的 Cloudflare tunnel 網址，重開 tunnel 換網址時要更新這裡並重啟 vite。
# 此檔被 .gitignore 的 *.local 忽略，不會進版控。
VITE_API_BASE_URL=https://triumph-processed-tooth-budget.trycloudflare.com
